import { BodyText } from 'components/Text'
import { Suspense } from 'react'
import { useSnapshot } from 'valtio'
import ContractListContainer from 'components/ContractListContainer'
import ProofStore from 'stores/ProofStore'
import ZkProof from 'components/ZkProof'

function ContractList() {
  const { proofsCompleted } = useSnapshot(ProofStore)

  return (
    <>
      {!!proofsCompleted?.length && (
        <ContractListContainer>
          {Array.from(proofsCompleted)
            .sort((a, b) => (a.account === b.account ? 0 : -1))
            .map((proof) => (
              <ZkProof
                proof={proof}
                contractAddress={proof.contract}
                key={proof.id}
              />
            ))}
        </ContractListContainer>
      )}
    </>
  )
}

export default function () {
  return (
    <Suspense
      fallback={
        <BodyText size="base">Fetching proofs generated by you...</BodyText>
      }
    >
      <ContractList />
    </Suspense>
  )
}
